(function(){
    // small JS starter (mobile nav toggle)
    document.addEventListener('DOMContentLoaded', function(){
    // nothing heavy — placeholder for future features
    console.log('iExtension Hair theme loaded');
    });
    })();